/**
 * Fleet home UI is composed in `app/FleetHomePage.tsx` (reporting dashboard).
 * This stub keeps the module route table readable; app replaces the element.
 */
export default function Dashboard() {
  return null;
}
