namespace CarTrack.Identity;

/// <summary>
/// Marker type identifying the CarTrack.Identity assembly.
///
/// Identity holds the shared identity foundation: ApplicationUser, JWT/token
/// services, the permission catalog and authorization handlers, and the
/// <c>ICurrentUserScope</c> abstraction. Real types are added in Phase 1.
/// </summary>
public sealed class IdentityAssemblyMarker;
